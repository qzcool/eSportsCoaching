<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.esports.web.dao.GamerMapper">

    <insert id="insertUser" parameterType="com.esports.web.model.UserProfile">
        insert into user_profile (userid,nickname,img,game_label,introduction) values (#{userid},#{nickname}, #{img}, #{gameLabel}, #{introduction})
         on duplicate key update update_time = now()
    </insert>

</mapper>